<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><% title || 'SCD | Normativa' %></title>
    <link rel="icon" href="#">
    <link rel="stylesheet" href="#">
    <link rel="stylesheet" href="#">
    <link rel="stylesheet" href="#">
</head>
<body>
    <div class="container">
        <!-- Sidebar -->
        <%- include('../partials/sidebar.ejs', { usuario: usuario, paginaActiva: 'normativa' }) %>

        <main class="content">
            <h1>Normatividad</h1>
            <p>Antes de continuar, revisa la siguiente documentacion.</p>

            <!-- Lista de documentos -->
            <%- include('../partials/listaDoc', { documentos: documentos }) %>
        </main>
    </div>

    <!-- Modal perfil -->
    <%- include('../partials/modalPerfil', { usuario: usuario }) %>

    <!-- Scripts -->
    <script src="/public/js/sidebar.js"></script>
    <script src="/public/js/modalPerfil.js"></script>
</body>
</html>